import { Router } from 'express';
// import authenticated from '../../middleware/authenticated';s

export default function logoutRoute(router: Router) {
	// router.post('/logout', authenticated(), async (req, res) => {
	// 	(req.session).destroy();
	// 	res.sendStatus(200);
	// });
};
